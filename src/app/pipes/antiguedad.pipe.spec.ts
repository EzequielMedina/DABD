import { AntiguedadPipe } from './antiguedad.pipe';

describe('AntiguedadPipe', () => {
  it('create an instance', () => {
    const pipe = new AntiguedadPipe();
    expect(pipe).toBeTruthy();
  });
});
